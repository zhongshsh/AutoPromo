
  # Academic Paper Promotion App

  This is a code bundle for Academic Paper Promotion App. The original project is available at https://gig-line-21091061.figma.site

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  